# Backend Notes

Backend setup and usage are documented in the repository root README:

- `../README.md`

Quick start:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```
